{
  "name": "@moonshot-marketing/creator-testing",
  "publishConfig": {
    "registry": "http://nexus.moonshot-marketing.com:8081/repository/moonshot-npm/"
  },
  "version": "1.0.0",
  "main": "./lib/index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn tslint && tsc --declaration",
    "tslint": "tslint -c tslint.json -p tsconfig.json --fix",
    "release:only": "yarn build && yarn publish --patch",
    "release:local": "yarn build && yalc publish",
    "git:push": "git pull origin master && git add . && git commit -m \"creator-utils new libraries\" && git push origin master",
    "release": "yarn install && yarn build && yarn version --patch --no-git-tag-version && yarn publish --non-interactive && yarn git:push"
  },
  "dependencies": {
    "typeorm": "^0.3.12",
    "@moonshot-marketing/creator-api-shared-entities": "*",
    "typeorm-faker": "^1.3.11"
  },
  "devDependencies": {
    "factory.ts": "^1.3.0",
    "@types/node": "^18.11.9",
    "@faker-js/faker": "^7.6.0",
    "@types/faker": "^6.6.9",
    "ts-node": "^10.4.0",
    "tslint": "^6.1.3",
    "typescript": "^4.9.3"
  }
}
